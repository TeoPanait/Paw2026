<!DOCTYPE html>
<html lang="ro">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - News Portal</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
          rel="stylesheet" />
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" asp-controller="Home" asp-action="Index">
                News Portal
            </a>
            <div class="navbar-nav">
                <a class="nav-link" asp-controller="Home" asp-action="Index">Acasă</a>
                <a class="nav-link" asp-controller="Articles" asp-action="Index">Articole</a>
                <a class="nav-link" asp-controller="Articles" asp-action="Create">Articol nou</a>
            </div>

            <div class="navbar-nav ms-auto align-items-center">
                @if (User.Identity?.IsAuthenticated == true)
                {
                    <span class="nav-link text-success fw-bold">@User.Identity.Name?.Split('@')[0]</span>
                    <form asp-controller="Auth" asp-action="Logout" method="post" class="d-inline">
                        <button type="submit" class="nav-link btn btn-link border-0">Sign Out</button>
                    </form>
                }
                else
                {
                    <span class="nav-link text-secondary fst-italic me-2">Vizitator</span>
                    <a class="nav-link" asp-controller="Auth" asp-action="Login">Log in</a>
                    <a class="nav-link" asp-controller="Auth" asp-action="Register">Cont nou</a>
                }
            </div>
        </div>
    </nav>

    <main class="container mt-4">
        @RenderBody()
    </main>

    <footer class="container mt-5 mb-3 text-muted text-center">
        <hr />
        <p>&copy; @DateTime.Now.Year - News Portal</p>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
